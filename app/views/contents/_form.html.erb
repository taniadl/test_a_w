  <%= simple_form_for([@project, @content], remote: true)  do |f| %>
    <%= f.input :title %>
    <%= f.label :type  %>
    <%= f.select :type, Content.types%>
    <%= f.button :submit %>
  <% end %>
