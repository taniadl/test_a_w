  <%= simple_form_for([@project , @project.contents.find(params[:id])]) do |f| %>
    <%= f.input :title %>
    <%= f.label :type  %>
    <%= f.select :type, Content.types%>
    <%= f.button :submit %>
  <% end %>

